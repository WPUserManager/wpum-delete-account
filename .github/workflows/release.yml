name: Release

on:
  push:
    tags: ["v[0-9]+.[0-9]+.[0-9]+"]

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  deploy:
    uses: WPUserManager/.github/.github/workflows/deploy.yml@master
    with:
      plugin-slug: wpum-delete-account
    secrets: inherit
